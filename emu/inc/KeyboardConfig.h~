#pragma once

#include "mbed.h"

typedef enum {
	KB_SPACE = A3,
	KB_ESCAPE = PA_11,
    KB_RETURN = NULL,
    KB_CTRL = NULL,
    KB_q = PB_2,
    KB_w = NULL,
    KB_e = PB_12,
    KB_r = NULL,
    KB_t = NULL,
    KB_y = NULL,
    KB_u = NULL,
    KB_i = NULL,
    KB_o = NULL,
    KB_p = NULL,
    KB_a = NULL,
    KB_s = NULL,
    KB_d = NULL,
    KB_f = NULL,
    KB_g = NULL,
    KB_h = NULL,
    KB_j = NULL,
    KB_k = NULL,
    KB_l = NULL,
    KB_z = NULL,
    KB_x = NULL,
    KB_c = PC_13,
    KB_v = PB_11,
    KB_b = NULL,
    KB_n = NULL,
    KB_m = NULL,
}KB;
